#include "global.h"

bool allowCaching = true;

//Does currenly not work because for some reason, KHE does not support backpointers
//during solution phase
const bool useBackPointerForNumIds = false;
